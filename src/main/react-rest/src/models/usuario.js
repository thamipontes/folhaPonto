export class Usuario {
    constructor(login, nomeCompleto, senha, role ){
        this.login = login;
        this.nomeCompleto = nomeCompleto;
        this.senha = senha;
        this.role = role;
    }
}